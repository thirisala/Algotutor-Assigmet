public class Main
{
	public static void main(String[] args) 
	{
		int a=7,b=6;
		System.out.println(gcd(a,b));
	}
	private static int gcd(int a,int b)
	{
	if(b==0)
		return a;
	return gcd(b,a%b);
	}
}
