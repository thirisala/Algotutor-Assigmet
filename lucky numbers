public class Main
{
     public static void main(String[] args)
	 {
        int a=12,ans=0;
        for(int i=1;i<=a;i++)
        {
           int count=0;
           for(int j=2;j<=a;j++)
           {
              if(i%j==0)
              {
                count++;
              }
           }
           if(count==2)
           {
              ans++;
           }
        }
        System.out.print(ans);
     }
}
